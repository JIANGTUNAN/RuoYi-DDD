/**
 * 接口层（API Layer）
 * 职责：负责出入相关的一切事务，本身不包含任何业务逻辑
 * <p>
 * ================================================================================
 * 核心功能
 * ================================================================================
 * <p>
 * 1. 控制器
 * 1.1. 每个功能对应一个控制器，接收 HTTP 请求
 * 2. 参数校验
 * 2.1. 入参合法性校验（@Valid、@NotNull 等）
 * 3. 统一响应
 * 3.1. 封装标准响应格式（Result<T>）
 * 4. 异常处理
 * 4.1. 全局异常捕获与错误响应
 * <p>
 * ================================================================================
 * 网络配置
 * ================================================================================
 * <p>
 * - 跨域配置（CORS）
 * - 身份校验（Sa-Token 拦截器）
 * - 上下文初始化（请求上下文、用户信息）
 * - 过滤器、拦截器
 * <p>
 * ================================================================================
 * 设计原则
 * ================================================================================
 * <p>
 * - 仅负责请求接收与响应返回
 * - 不做业务逻辑，业务委托给应用层
 * - DTO 与领域对象隔离，避免暴露内部模型
 *
 * @author tooolan
 * @since 2026年4月7日
 */
package com.ruoyi.ddd.api;
